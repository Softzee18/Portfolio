# Server configuration
PORT=3000
# NODE_ENV=development

# MongoDB Atlas connection (replace <db_password> with your DB user's password)
# Atlas gave you a connection string — insert your password and specify the DB name (e.g. "portfolio").
# If your password contains special characters (like @, /, #) URL-encode it first.
# Example final form:
# mongodb+srv://portfolio_admin:MyP%40ss123@portfolio.8w1lyqe.mongodb.net/portfolio?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://portfolio_admin:<db_password>@portfolio.8w1lyqe.mongodb.net/?appName=portfolio

# JWT secret for admin authentication (change this!)
JWT_SECRET=your-super-secret-jwt-key-change-me-in-production

# reCAPTCHA configuration
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key
RECAPTCHA_SITE_KEY=your-recaptcha-site-key

# Admin credentials (change these!)
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="admin@1"

# CORS origins (add your frontend URL in production)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000,https://https://digitalservices1.netlify.app/